[project]
name = "trimmy"
version = "0.2.0"
description = "Trimmy — vertical split crop video editor"
requires-python = ">=3.11"
dependencies = ["PySide6>=6.5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
trimmy = "video_trimmer.__main__:main"
